;; Dimensional Analysis at compiletime

(ns units2.typed
  (:require [units2.core :refer :all]
            [units2.ops :as ops]
            ;core.typed, etc ...
            )
)

(set! *warn-on-reflection* true)
